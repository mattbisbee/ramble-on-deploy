{{#each this}}
<br>
<form class="comment form-group">
  
  <div class="meta">
  {{user.username}} on {{format_date created_at}}
  </div>

  <div class="text">
  {{comment_text}}
  </div>
</form>
{{/each}}